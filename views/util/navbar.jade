#navbar
    .title Breakfast
    if locals.user
      .user.logged-in
        a(href='/logout/') Logout
        .email #{locals.user.emailAddress}
    else
      .user
        a(href='/login/') Login
